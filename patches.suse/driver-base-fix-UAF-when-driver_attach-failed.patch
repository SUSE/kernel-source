From: Schspa Shi <schspa@gmail.com>
Date: Fri, 13 May 2022 19:24:44 +0800
Subject: [PATCH] driver: base: fix UAF when driver_attach failed
Git-commit: 310862e574001a97ad02272bac0fd13f75f42a27
Patch-mainline: v5.19-rc1
References: CVE-2022-49385 bsc#1237951

When driver_attach(drv); failed, the driver_private will be freed.
But it has been added to the bus, which caused a UAF.

To fix it, we need to delete it from the bus when failed.

Fixes: 190888ac01d0 ("driver core: fix possible missing of device probe")
Signed-off-by: Schspa Shi <schspa@gmail.com>
Link: https://lore.kernel.org/r/20220513112444.45112-1-schspa@gmail.com
Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
Signed-off-by: David Sterba <dsterba@suse.com>
---
 drivers/base/bus.c |    4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

--- a/drivers/base/bus.c
+++ b/drivers/base/bus.c
@@ -683,7 +683,7 @@ int bus_add_driver(struct device_driver
 	if (drv->bus->p->drivers_autoprobe) {
 		error = driver_attach(drv);
 		if (error)
-			goto out_unregister;
+			goto out_del_list;
 	}
 	module_add_driver(drv->owner, drv);
 
@@ -710,6 +710,8 @@ int bus_add_driver(struct device_driver
 
 	return 0;
 
+out_del_list:
+	klist_del(&priv->knode_bus);
 out_unregister:
 	kobject_put(&priv->kobj);
 	/* drv->p is freed in driver_release()  */
